<UserControl x:Class="SkyWingViewer.Views.TagListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkyWingViewer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--将来的にはタグ一覧の画面の左側では、お気に入りやタググループ等メニューを追加するかも-->
        <ListView ItemsSource="{Binding TagList}"
                Grid.Column="1"
                Background="Transparent"
                BorderThickness="0">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- 自動で折り返す -->
                    <!--<WrapPanel Orientation="Horizontal" />-->
                    <!--2 列で表示する-->
                    <UniformGrid Columns="2" VerticalAlignment="Top"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <!--ダブルクリック時の動作-->
                    <!--<EventSetter Event="MouseDoubleClick" Handler="ListViewItemMouseDoubleClick"/>-->
                    <!--右クリックの操作-->
                    <Setter Property="ContextMenu" Value="{StaticResource AssetContextMenu}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Model.TagName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
