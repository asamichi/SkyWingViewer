<UserControl x:Class="SkyWingViewer.Views.StarRatingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkyWingViewer.Views"
             xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--IStarRating を継承した ViewModel に対応-->
    <StackPanel Orientation="Horizontal">
        <ItemsControl ItemsSource="{Binding Stars}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <!--ボタンは ChangeRateCommand に、自身の StarIndex を渡す-->
                    <Button 
                        Command="{Binding DataContext.ChangeRateCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding StarIndex}"
                        BorderThickness="0"
                        Background="Transparent"
                        Focusable="False"
                        Margin="0"
                        Padding="2">
                        <ic:FluentIcon x:Name="RateStar" Icon="Star" FontSize="14" IconSize="Size16"/>
                    </Button>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsFilled}" Value="True">
                            <Setter TargetName="RateStar" Property="IconVariant" Value="Color"/>
                            <Setter TargetName="RateStar" Property="Foreground" Value="Yellow"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
