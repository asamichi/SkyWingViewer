<UserControl x:Class="SkyWingViewer.Views.SubAreaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkyWingViewer.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="20"/> <!--splitter-->
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl Content="{Binding InformationTagViewModel}" Grid.Row="0"/>
        <Border Height="1" Background="#404040" SnapsToDevicePixels="True" Margin="0,5" Grid.Row="1" />
        <ContentControl Content="{Binding MemoViewModel}" Grid.Row="2"/>
        <GridSplitter  HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="5" Grid.Row="3" Cursor="SizeNS"/>
        <!--<Border Height="1" Background="#404040" SnapsToDevicePixels="True" VerticalAlignment="Center" Margin="0,5" Grid.Row="4" />-->
        <ContentControl Content="{Binding AssetInformationViewModel}" Grid.Row="5"/>

    </Grid>
</UserControl>
