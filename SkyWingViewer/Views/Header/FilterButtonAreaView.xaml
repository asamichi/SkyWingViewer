<UserControl x:Class="SkyWingViewer.Views.FilterButtonAreaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkyWingViewer.Views"
             mc:Ignorable="d" 
             xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Command="{Binding FilterButtonCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Self}}">
            <ic:FluentIcon Icon="Filter" IconSize="Size16" FontSize="16" Grid.Column="0"/>
        </Button>

        <!-- 重ねる数字（適用しているフィルターの数） -->
        <!--IsHitTestVisible="False" とするとクリックの判定が下の要素に透過する-->
        <Border Background="#119EDA" 
                CornerRadius="8" 
                MinWidth="16" 
                MinHeight="16"
                HorizontalAlignment="Right" 
                VerticalAlignment="Top"
                Margin="0,-8,-8,0"
                IsHitTestVisible="False">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SearchTagNum}" Value="0">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding SearchTagNum}" 
                       Foreground="white" 
                       FontSize="10" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Padding="3,0"/>
        </Border>

        <local:StarRatingView DataContext="{Binding StarRatingFilterViewModel}"  HorizontalAlignment="Left" Grid.Column="1"/>
    </Grid>
</UserControl>
