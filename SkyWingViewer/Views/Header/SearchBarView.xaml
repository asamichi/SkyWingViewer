<UserControl x:Class="SkyWingViewer.Views.SearchBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkyWingViewer.Views"
             xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ic:FluentIcon Icon="Search" IconSize="Size16" FontSize="16" Grid.Column="0"/>
        <TextBox x:Name="SearchTextBox" Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged,Delay=30}" Grid.Column="1"></TextBox>
    </Grid>
</UserControl>
